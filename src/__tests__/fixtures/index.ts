/**
 * Test Fixtures - Centralized mock data for all tests
 * 
 * Usage:
 *   import { mockStampMint, mockProUser, mockVaultStamps } from '../fixtures';
 * 
 * These fixtures provide consistent test data across all unit and component tests.
 * Update these files when the database schema changes.
 */

// Users & Auth
export * from './users';

// Vaults
export * from './vaults';

// Items (Stamps & Coins)
export * from './items';
